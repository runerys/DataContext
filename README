This project demonstrates a concept for isolating data: Data Context

It basically creates a vertical slice in the data model, and can be useful in integration tests as well as in scenario based applications.